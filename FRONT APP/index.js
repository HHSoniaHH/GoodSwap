import { AppRegistry } from 'react-native';
import 'react-native-gesture-handler';

// Remplacez 'NomDeVotreApp' par le nom de votre application
const appRootComponent = () => App;
AppRegistry.registerComponent('my-app', appRootComponent);
